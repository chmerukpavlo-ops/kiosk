# Чеклист запуску проєкту ✅

Використовуйте цей чеклист для швидкого запуску:

- [ ] **Крок 1:** Перевірив версію Node.js (`node --version` - має бути 18+)
- [ ] **Крок 2:** Встановив залежності (`npm run install:all`)
- [ ] **Крок 3:** Запустив PostgreSQL сервіс
- [ ] **Крок 4:** Створив базу даних `kiosk_db` в PostgreSQL
- [ ] **Крок 5:** Створив файл `backend/.env` з правильними налаштуваннями
- [ ] **Крок 6:** Запустив проєкт (`npm run dev`)
- [ ] **Крок 7:** Перевірив, що backend працює (http://localhost:3001/api/health)
- [ ] **Крок 8:** Відкрив frontend (http://localhost:5173)
- [ ] **Крок 9:** Увійшов в систему (admin / admin123)
- [ ] **Крок 10:** Створив перший ларьок
- [ ] **Крок 11:** Створив першого продавця
- [ ] **Крок 12:** Додав перші товари

**Готово! Проєкт працює! 🎉**

---

## Швидкі команди

```bash
# Встановлення
npm run install:all

# Запуск
npm run dev

# Перевірка backend
curl http://localhost:3001/api/health

# Перевірка PostgreSQL
pg_isready
```

## Швидка перевірка проблем

```bash
# Порт зайнятий?
lsof -ti:3001 | xargs kill  # Backend
lsof -ti:5173 | xargs kill  # Frontend

# Перевстановити залежності
rm -rf node_modules backend/node_modules frontend/node_modules
npm run install:all
```

