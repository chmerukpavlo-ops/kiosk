# 🚀 Простий деплой на Render + Vercel

Найпростіший спосіб задеплоїти проєкт без налаштування серверів!

## Що потрібно:

1. **GitHub** - твій код (якщо ще не там, завантаж)
2. **Render.com** - для Backend + База даних (безкоштовно)
3. **Vercel.com** - для Frontend (безкоштовно)

---

## Крок 1: Завантаж код на GitHub

Якщо код ще не на GitHub:

```bash
# В корені проєкту
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/твій-username/kiosk.git
git push -u origin main
```

Або завантаж через GitHub Desktop / веб-інтерфейс.

---

## Крок 2: Запускаємо Базу даних і Backend на Render

### 2.1. Зареєструйся на Render

1. Зайди на [dashboard.render.com](https://dashboard.render.com)
2. Натисни **"Get Started for Free"**
3. Увійди через **GitHub** (найпростіше)

### 2.2. Створи Базу Даних

1. Натисни **"New +"** → **"PostgreSQL"**
2. Заповни:
   - **Name:** `kiosk-db`
   - **Database:** `kiosk_db`
   - **User:** `kiosk_user`
   - **Plan:** **Free**
3. Натисни **"Create Database"**
4. ⚠️ **ВАЖЛИВО:** Коли база створиться, знайди розділ **"Connections"** і скопіюй **"Internal Database URL"** (воно типу `postgresql://kiosk_user:password@dpg-xxx/kiosk_db`)

### 2.3. Створи Backend

1. Натисни **"New +"** → **"Web Service"**
2. Підключи свій GitHub репозиторій
3. Заповни налаштування:
   - **Name:** `kiosk-backend`
   - **Region:** найближчий до тебе
   - **Branch:** `main` (або `master`)
   - **Root Directory:** `backend` ⚠️ **ВАЖЛИВО!**
   - **Runtime:** `Node`
   - **Build Command:** `npm install && npm run build`
   - **Start Command:** `npm start`
   - **Plan:** **Free**
4. Прокрути вниз до **"Environment Variables"** і додай:
   - `DATABASE_URL` = встав той **Internal Database URL**, що копіював вище
   - `JWT_SECRET` = придумай складний пароль (наприклад: `my_super_secret_jwt_key_12345`)
   - `NODE_ENV` = `production`
   - `PORT` = `10000` (Render використовує цей порт)
5. Натисни **"Create Web Service"**
6. Чекай 5-10 хвилин поки збирається
7. Коли засвітиться зеленим **"Live"** → скопіюй посилання на бекенд (воно зверху, типу `https://kiosk-backend-xyz.onrender.com`)

---

## Крок 3: Запускаємо Frontend на Vercel

### 3.1. Зареєструйся на Vercel

1. Зайди на [vercel.com](https://vercel.com)
2. Натисни **"Sign Up"**
3. Увійди через **GitHub**

### 3.2. Створи проєкт

1. Натисни **"Add New..."** → **"Project"**
2. Вибери свій репозиторій `kiosk` і натисни **"Import"**
3. Налаштування:
   - **Framework Preset:** Vite (він зазвичай сам вгадає)
   - **Root Directory:** Натисни **"Edit"** і вибери папку **`frontend`** ⚠️ **ВАЖЛИВО!**
   - **Build Command:** `npm run build` (або залиш як є)
   - **Output Directory:** `dist` (або залиш як є)
4. Прокрути вниз до **"Environment Variables"** і додай:
   - **Name:** `VITE_API_URL`
   - **Value:** Встав посилання на твій бекенд з Render (те, що копіював вище)
     - ⚠️ **ВАЖЛИВО:** Посилання має бути **БЕЗ** `/` в кінці!
     - Правильно: `https://kiosk-backend-xyz.onrender.com`
     - Неправильно: `https://kiosk-backend-xyz.onrender.com/`
5. Натисни **"Deploy"**

### 3.3. Готово! 🎉

Vercel збере проєкт за 1-2 хвилини і видасть тобі посилання типу:
`https://kiosk-frontend.vercel.app`

**Це і є твій сайт!** Можеш відкрити з телефону, скинути другу — все працює!

---

## Перевірка

1. Відкрий посилання від Vercel
2. Має відкритися сторінка входу
3. Увійди: `admin` / `admin123`
4. Все має працювати! ✅

---

## ⚠️ Важливі нюанси безкоштовного Render:

### "Сплячий режим"

Render вимикає твій бекенд, якщо ним ніхто не користується **15 хвилин**.

**Що це означає:**
- Коли ти вперше за день зайдеш на сайт, він може "тупити" і не завантажувати дані десь **50 секунд**
- Це сервер прокидається
- Наступні запити будуть літати швидко

**Рішення:** Просто зачекай після першого запиту.

### База даних

На безкоштовному тарифі база видаляється через **90 днів** неактивності.

Для тестів і навчання цього вистачить з головою!

---

## Оновлення проєкту

Коли хочеш оновити код:

1. Зроби зміни локально
2. Запуш на GitHub:
   ```bash
   git add .
   git commit -m "Update"
   git push
   ```
3. Render і Vercel автоматично перезберуть проєкт! 🎉

---

## Вирішення проблем

### Frontend не підключається до Backend

- Перевір, чи `VITE_API_URL` в Vercel правильний (без `/` в кінці)
- Перевір, чи Backend на Render "Live" (зелений статус)
- Перевір логи в Render: Dashboard → твій сервіс → Logs

### Backend не запускається

- Перевір логи в Render
- Перевір, чи `DATABASE_URL` правильний
- Перевір, чи `Root Directory` = `backend`

### База даних не підключається

- Переконайся, що використовуєш **Internal Database URL** (не External!)
- Перевір, чи база даних створена і активна

---

## Готово! 🚀

Тепер твій сайт доступний з будь-якого пристрою в інтернеті!

**Frontend:** `https://твій-проєкт.vercel.app`  
**Backend:** `https://твій-бекенд.onrender.com`

