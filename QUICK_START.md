# Покрокова інструкція запуску проєкту

## Крок 1: Перевірка вимог

Переконайтеся, що встановлено:
- **Node.js** версії 18 або вище
- **PostgreSQL** (або можна використати SQLite для тестування)
- **npm** або **yarn**

Перевірка версії Node.js:
```bash
node --version
```

Якщо Node.js не встановлено, завантажте з [nodejs.org](https://nodejs.org/)

### Швидка перевірка системи

Виконайте скрипт для автоматичної перевірки:
```bash
./check-system.sh
```

Або вручну перевірте кожен пункт нижче.

---

## Крок 2: Встановлення залежностей

Відкрийте термінал в кореневій папці проєкту (`/Users/macbook/kiosk`) та виконайте:

```bash
npm run install:all
```

Ця команда встановить залежності для:
- Кореневого проєкту
- Backend
- Frontend

**Час виконання:** 2-5 хвилин (залежить від швидкості інтернету)

---

## Крок 3: Налаштування бази даних PostgreSQL

### 3.1. Запустіть PostgreSQL

Якщо PostgreSQL встановлено локально:
```bash
# macOS (через Homebrew)
brew services start postgresql

# Або перевірте, чи працює сервіс
pg_isready
```

### 3.2. Створіть базу даних

Підключіться до PostgreSQL:
```bash
psql postgres
```

Або через інший клієнт (pgAdmin, DBeaver тощо).

Створіть базу даних:
```sql
CREATE DATABASE kiosk_db;
```

Перевірте створення:
```sql
\l
```

Вийдіть з psql:
```sql
\q
```

### 3.3. Створіть файл з налаштуваннями

Створіть файл `backend/.env` в папці `backend/`:

```bash
cd backend
touch .env
```

Відкрийте файл `.env` та додайте:

```env
PORT=3001
JWT_SECRET=your-super-secret-key-change-in-production-12345
DATABASE_URL=postgresql://ваш_користувач:ваш_пароль@localhost:5432/kiosk_db
NODE_ENV=development
```

**Приклад для стандартного налаштування PostgreSQL:**
```env
PORT=3001
JWT_SECRET=my-secret-key-12345
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/kiosk_db
NODE_ENV=development
```

**Важливо:** Замініть `ваш_користувач` та `ваш_пароль` на ваші реальні дані доступу до PostgreSQL.

---

## Крок 4: Запуск проєкту

### Варіант А: Запуск всього проєкту одразу (рекомендовано)

В кореневій папці проєкту виконайте:

```bash
npm run dev
```

Ця команда одночасно запустить:
- Backend на порту 3001
- Frontend на порту 5173

### Варіант Б: Запуск окремо (якщо потрібен контроль)

**Термінал 1 - Backend:**
```bash
cd backend
npm run dev
```

**Термінал 2 - Frontend:**
```bash
cd frontend
npm run dev
```

---

## Крок 5: Перевірка роботи

### 5.1. Перевірка Backend

**Важливо:** Backend - це API сервер, а не веб-сайт. Він не має HTML сторінок.

Перевірка через curl або в браузері:
```
http://localhost:3001/api/health
```

Має повернутися: `{"status":"ok"}`

Або відкрийте кореневий шлях:
```
http://localhost:3001
```

Має показати JSON з інформацією про API та доступні endpoints.

### 5.2. Перевірка Frontend

**Це і є ваш веб-сайт!** Відкрийте в браузері:
```
http://localhost:5173
```

Має відкритися сторінка входу з формою для авторизації.

**Примітка:** 
- Backend (http://localhost:3001) - це API сервер для обробки даних
- Frontend (http://localhost:5173) - це веб-сайт, який ви бачите в браузері

### 5.3. Перевірка бази даних

Backend автоматично створить всі необхідні таблиці при першому запуску.

Перевірте в PostgreSQL:
```sql
\c kiosk_db
\dt
```

Має показати таблиці: `kiosks`, `users`, `products`, `sales`, `schedule`

---

## Крок 6: Вхід в систему

1. Відкрийте http://localhost:5173
2. Введіть дані для входу:
   - **Логін:** `admin`
   - **Пароль:** `admin123`
3. Натисніть "Увійти"

Після входу ви побачите дашборд адміністратора.

### ⚠️ Якщо виникає помилка входу:

**Перевірте:**
1. Чи запущений backend (має бути повідомлення в терміналі про створення admin користувача)
2. Чи працює backend API:
   ```bash
   curl http://localhost:3001/api/health
   ```
3. Перевірте логи backend - має бути:
   ```
   ✅ Default admin user created (username: admin, password: admin123)
   ```

**Детальна інструкція з вирішення проблем:** [TROUBLESHOOTING.md](./TROUBLESHOOTING.md)

---

## Крок 7: Перші кроки після входу

### Для адміністратора:

1. **Створіть ларьок:**
   - Перейдіть на "Ларьки"
   - Натисніть "+ Додати ларьок"
   - Заповніть назву та адресу

2. **Створіть продавця:**
   - Перейдіть на "Продавці"
   - Натисніть "+ Додати продавця"
   - Заповніть ПІБ, логін, пароль
   - Призначте до ларька

3. **Додайте товари:**
   - Перейдіть на "Товари"
   - Натисніть "+ Додати товар"
   - Заповніть інформацію про товар

---

## Можливі проблеми та рішення

### Проблема: "Cannot connect to database"

**Рішення:**
1. Перевірте, чи запущений PostgreSQL:
   ```bash
   pg_isready
   ```
2. Перевірте правильність даних в `backend/.env`
3. Перевірте, чи існує база даних `kiosk_db`

### Проблема: "Port 3001 already in use"

**Рішення:**
1. Змініть порт в `backend/.env`:
   ```env
   PORT=3002
   ```
2. Або зупиніть процес, який використовує порт:
   ```bash
   lsof -ti:3001 | xargs kill
   ```

### Проблема: "Port 5173 already in use"

**Рішення:**
1. Vite автоматично запропонує інший порт
2. Або зупиніть процес:
   ```bash
   lsof -ti:5173 | xargs kill
   ```

### Проблема: "Module not found"

**Рішення:**
1. Переконайтеся, що встановлено всі залежності:
   ```bash
   npm run install:all
   ```
2. Якщо проблема залишається:
   ```bash
   rm -rf node_modules backend/node_modules frontend/node_modules
   npm run install:all
   ```

### Проблема: "JWT_SECRET is not defined"

**Рішення:**
1. Переконайтеся, що файл `backend/.env` існує
2. Перевірте, чи в ньому є `JWT_SECRET`

---

## Альтернатива: SQLite (для швидкого тестування)

Якщо не хочете налаштовувати PostgreSQL, можна використати SQLite:

1. Встановіть пакет:
   ```bash
   cd backend
   npm install better-sqlite3
   ```

2. Змініть `backend/src/db/init.ts` для використання SQLite замість PostgreSQL

**Примітка:** Для продакшн використання рекомендовано PostgreSQL.

---

## Готово! 🎉

Якщо все працює, ви побачите:
- ✅ Backend запущений на http://localhost:3001
- ✅ Frontend запущений на http://localhost:5173
- ✅ Можете увійти як адміністратор
- ✅ База даних створена та заповнена тестовим користувачем

---

## Корисні команди

```bash
# Зупинити всі процеси
# Натисніть Ctrl+C в терміналі

# Перезапустити проєкт
npm run dev

# Перевірити логи backend
# Логи відображаються в терміналі, де запущений backend

# Очистити кеш та перевстановити залежності
rm -rf node_modules backend/node_modules frontend/node_modules package-lock.json
npm run install:all
```

