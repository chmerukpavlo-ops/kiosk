# 🚀 Інструкція по деплою на сервер

Ця інструкція допоможе задеплоїти проект на безкоштовні хостинги для тестування PWA з телефону.

## 📋 Передумови

1. GitHub репозиторій з кодом
2. Аккаунти на [Render.com](https://render.com) та [Vercel.com](https://vercel.com)

## 🔧 Крок 1: Деплой Backend на Render.com

1. **Зареєструйтеся/увійдіть на [Render.com](https://render.com)**

2. **Створіть новий Web Service:**
   - Натисніть "New +" → "Web Service"
   - Підключіть ваш GitHub репозиторій
   - Оберіть репозиторій з проектом

3. **Налаштування сервісу:**
   - **Name**: `kiosk-backend`
   - **Environment**: `Node`
   - **Build Command**: `cd backend && npm install && npm run build`
   - **Start Command**: `cd backend && npm start`
   - **Plan**: `Free`

4. **Створіть базу даних:**
   - Натисніть "New +" → "PostgreSQL"
   - **Name**: `kiosk-db`
   - **Database**: `kiosk_db`
   - **User**: `kiosk_user`
   - **Plan**: `Free`

5. **Налаштуйте Environment Variables:**
   - `NODE_ENV` = `production`
   - `PORT` = `10000` (Render автоматично призначає порт)
   - `DATABASE_URL` = (автоматично з бази даних, або скопіюйте Internal Database URL)
   - `JWT_SECRET` = (згенеруйте складний пароль, мінімум 32 символи)
     ```bash
     # Можна згенерувати через:
     openssl rand -base64 32
     ```
   - `TELEGRAM_BOT_TOKEN` = (опціонально, якщо використовуєте Telegram)

6. **Збережіть і дочекайтеся деплою**

7. **Скопіюйте URL вашого backend** (наприклад: `https://kiosk-backend.onrender.com`)

## 🌐 Крок 2: Деплой Frontend на Vercel.com

1. **Зареєструйтеся/увійдіть на [Vercel.com](https://vercel.com)**

2. **Імпортуйте проект:**
   - Натисніть "Add New..." → "Project"
   - Підключіть ваш GitHub репозиторій
   - Оберіть репозиторій

3. **Налаштування проекту:**
   - **Framework Preset**: `Vite`
   - **Root Directory**: `frontend` ⚠️ **ОБОВ'ЯЗКОВО!**
   - **Build Command**: (залиште порожнім - використовується з `vercel.json`)
   - **Output Directory**: (залиште порожнім - використовується з `vercel.json`)
   - **Install Command**: (залиште порожнім - використовується з `vercel.json`)
   
   ⚠️ **ВАЖЛИВО:** Після встановлення Root Directory як `frontend`, Vercel автоматично буде працювати з цієї папки, тому команди в `vercel.json` не містять `cd frontend &&`!

4. **Environment Variables:**
   - `VITE_API_URL` = `https://ваш-backend-url.onrender.com`
     (Наприклад: `https://kiosk-backend.onrender.com`)

5. **Deploy!**

6. **Скопіюйте URL вашого frontend** (наприклад: `https://kiosk-app.vercel.app`)

## ✅ Крок 3: Перевірка

1. **Відкрийте frontend URL у браузері**
2. **Увійдіть:**
   - Логін: `admin`
   - Пароль: `admin123`

3. **Перевірте PWA:**
   - На мобільному телефоні відкрийте frontend URL
   - У браузері (Chrome/Safari) натисніть "Меню" → "Додати на головний екран"
   - Або дочекайтеся автоматичного підказки для встановлення

## 🔒 Важливо для PWA

- **HTTPS обов'язковий** для PWA (Vercel та Render надають HTTPS автоматично)
- **Service Worker** працює тільки на HTTPS або localhost
- **CORS** налаштований автоматично через `cors` middleware

## 🐛 Вирішення проблем

### Backend не запускається
- Перевірте логи на Render.com
- Переконайтеся, що `DATABASE_URL` правильний
- Перевірте, що `JWT_SECRET` встановлений

### Frontend не підключається до Backend
- Перевірте `VITE_API_URL` у Vercel Environment Variables
- Переконайтеся, що backend URL правильний (без `/api` в кінці)
- Перевірте CORS налаштування в backend

### PWA не встановлюється
- Переконайтеся, що використовується HTTPS
- Перевірте, що `manifest.json` доступний
- Перевірте консоль браузера на помилки

## 📱 Тестування на телефоні

1. Відкрийте frontend URL на телефоні
2. Увійдіть в систему
3. Натисніть "Встановити додаток" (якщо з'явиться підказка)
4. Або через меню браузера: "Додати на головний екран"
5. Відкрийте додаток з домашнього екрану
6. Перевірте, що все працює

## 🔄 Оновлення

Після кожного push в GitHub:
- **Render** автоматично перезбирає backend
- **Vercel** автоматично перезбирає frontend

Просто зачекайте кілька хвилин після push!

## 💡 Поради

- Використовуйте **безкоштовні плани** для тестування
- **Render Free** може "засинати" після 15 хвилин неактивності (перший запит може бути повільним)
- **Vercel Free** має обмеження на кількість деплоїв, але достатньо для тестування
- Для production використовуйте платні плани або власні сервери

