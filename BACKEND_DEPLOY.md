# 🚀 Покрокова інструкція: Деплой Backend на Render.com

## Крок 1: Підготовка

1. **Переконайтеся, що ваш код на GitHub:**
   ```bash
   git add .
   git commit -m "Prepare for deployment"
   git push origin main
   ```

## Крок 2: Створення аккаунту на Render.com

1. Відкрийте [https://render.com](https://render.com)
2. Натисніть **"Get Started for Free"**
3. Зареєструйтеся через GitHub (рекомендовано)

## Крок 3: Створення PostgreSQL бази даних

1. На головній сторінці Render натисніть **"New +"** → **"PostgreSQL"**
2. Заповніть форму:
   - **Name**: `kiosk-db` (або будь-яка назва)
   - **Database**: `kiosk_db`
   - **User**: `kiosk_user`
   - **Region**: оберіть найближчий (наприклад, `Frankfurt`)
   - **PostgreSQL Version**: `16` (або остання)
   - **Plan**: `Free` (для тестування)
3. Натисніть **"Create Database"**
4. **ВАЖЛИВО:** Дочекайтеся, поки база даних створиться (2-3 хвилини)
5. Після створення:
   - Перейдіть в налаштування бази даних
   - Знайдіть **"Internal Database URL"** (виглядає як `postgresql://kiosk_user:password@dpg-xxxxx-a.frankfurt-postgres.render.com/kiosk_db`)
   - **Скопіюйте цей URL** - він знадобиться для наступного кроку

## Крок 4: Створення Web Service (Backend)

1. На головній сторінці Render натисніть **"New +"** → **"Web Service"**
2. Підключіть ваш GitHub репозиторій:
   - Натисніть **"Connect account"** (якщо ще не підключено)
   - Оберіть ваш репозиторій з проектом
   - Натисніть **"Connect"**

3. **Налаштування сервісу:**
   - **Name**: `kiosk-backend` (або будь-яка назва)
   - **Region**: оберіть той самий, що й для бази даних
   - **Branch**: `main` (або ваша основна гілка)
   - **Root Directory**: `backend` ⚠️ **ОБОВ'ЯЗКОВО!**
   - **Runtime**: `Node`
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm start`
   - **Plan**: `Free` (для тестування)

4. **Environment Variables** (натисніть **"Advanced"** → **"Add Environment Variable"**):
   
   Додайте наступні змінні:
   
   ```
   NODE_ENV = production
   PORT = 10000
   DATABASE_URL = (вставте Internal Database URL з кроку 3)
   JWT_SECRET = (згенеруйте складний пароль, див. нижче)
   ```
   
   **Як згенерувати JWT_SECRET:**
   ```bash
   # На macOS/Linux:
   openssl rand -base64 32
   
   # Або використайте онлайн генератор:
   # https://www.random.org/strings/
   # Мінімум 32 символи!
   ```
   
   **Приклад JWT_SECRET:**
   ```
   aB3xK9mP2qR7vT5wY8zA1bC4dE6fG8hI0jK2lM4nO6pQ8rS0tU2vW4xY6zA8
   ```

5. **Підключення до бази даних:**
   - В розділі **"Database"** натисніть **"Link Database"**
   - Оберіть базу даних, яку ви створили в кроці 3
   - Render автоматично додасть `DATABASE_URL` (але переконайтеся, що він є в Environment Variables)

6. Натисніть **"Create Web Service"**

## Крок 5: Очікування деплою

1. Render почне збирати та деплоїти ваш backend
2. Це займе **5-10 хвилин** (перший раз)
3. Ви побачите логи збірки в реальному часі
4. Після успішного деплою ви побачите:
   ```
   ✅ Your service is live at https://kiosk-backend.onrender.com
   ```

## Крок 6: Перевірка роботи

1. **Скопіюйте URL вашого backend** (наприклад: `https://kiosk-backend.onrender.com`)
2. Відкрийте в браузері: `https://ваш-backend-url.onrender.com/api/auth/me`
3. Має з'явитися помилка `401 Unauthorized` - це нормально, означає що API працює!
4. Якщо бачите помилку `Cannot GET /api/auth/me` - перевірте логи на Render

## Крок 7: Налаштування Frontend

1. Перейдіть в **Vercel** → ваш проект
2. Відкрийте **Settings** → **Environment Variables**
3. Додайте/оновіть:
   ```
   VITE_API_URL = https://ваш-backend-url.onrender.com
   ```
   ⚠️ **ВАЖЛИВО:** Без `/api` в кінці!
4. Зробіть **Redeploy** frontend

## 🐛 Вирішення проблем

### Backend не запускається

**Перевірте логи:**
1. На Render відкрийте ваш Web Service
2. Натисніть вкладку **"Logs"**
3. Шукайте помилки (червоний текст)

**Найчастіші проблеми:**

1. **"Cannot find module"**:
   - Переконайтеся, що `Root Directory` = `backend`
   - Переконайтеся, що `package.json` в папці `backend/`

2. **"Database connection error"**:
   - Перевірте `DATABASE_URL` в Environment Variables
   - Переконайтеся, що використовується **Internal Database URL** (не External!)
   - Формат: `postgresql://user:password@host:port/database`

3. **"JWT_SECRET is not defined"**:
   - Переконайтеся, що `JWT_SECRET` додано в Environment Variables
   - Переконайтеся, що значення містить мінімум 32 символи

4. **"Port already in use"**:
   - Переконайтеся, що `PORT = 10000` в Environment Variables
   - Render автоматично призначає порт через змінну `PORT`

### База даних не ініціалізується

1. Перевірте логи на Render - мають бути повідомлення про ініціалізацію
2. Якщо помилка - перевірте `DATABASE_URL`
3. Можна вручну підключитися до бази через **Render Shell**:
   - Відкрийте базу даних → **"Connect"** → **"Render Shell"**
   - Виконайте SQL команди вручну

### CORS помилки

1. Перевірте, що в `backend/src/index.ts` є:
   ```typescript
   app.use(cors({
     origin: '*', // або конкретний URL frontend
     credentials: true,
   }));
   ```

## ✅ Чеклист після деплою

- [ ] Backend доступний за URL (відкривається в браузері)
- [ ] API endpoint `/api/auth/me` повертає помилку 401 (не 404!)
- [ ] Логи на Render показують "Server running on port..."
- [ ] `VITE_API_URL` встановлено в Vercel
- [ ] Frontend може підключитися до backend

## 📝 Додаткові налаштування

### Автоматичний деплой з GitHub

Render автоматично деплоїть при кожному push в `main` гілку. Просто:
```bash
git push origin main
```

### Оновлення Environment Variables

1. Відкрийте ваш Web Service на Render
2. **Settings** → **Environment**
3. Додайте/змініть змінні
4. Натисніть **"Save Changes"**
5. Render автоматично перезапустить сервіс

### Перегляд логів

1. Відкрийте ваш Web Service
2. Вкладка **"Logs"**
3. Ви побачите всі логи в реальному часі

## 💡 Поради

- **Free план на Render** може "засинати" після 15 хвилин неактивності
- Перший запит після "засинання" може займати 30-60 секунд
- Для production використовуйте платний план або власний сервер
- Зберігайте `JWT_SECRET` в безпеці - не комітьте в GitHub!

## 🎉 Готово!

Після успішного деплою ваш backend буде доступний за URL типу:
`https://kiosk-backend.onrender.com`

Скопіюйте цей URL і додайте в `VITE_API_URL` в Vercel для frontend!

