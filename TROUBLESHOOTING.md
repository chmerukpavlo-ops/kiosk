# Вирішення проблем з входом

## Помилка входу: "Невірний логін або пароль"

### Крок 1: Перевірте, чи запущений backend

Відкрийте термінал та перевірте:

```bash
# Перевірте, чи працює backend
curl http://localhost:3001/api/health
```

**Очікуваний результат:** `{"status":"ok"}`

**Якщо не працює:**
1. Перевірте, чи запущений backend:
   ```bash
   cd backend
   npm run dev
   ```
2. Перевірте логи - має бути повідомлення:
   ```
   ✅ Database initialized successfully
   ✅ Default admin user created (username: admin, password: admin123)
   🚀 Server running on http://localhost:3001
   ```

### Крок 2: Перевірте підключення до бази даних

**Проблема:** Backend не може підключитися до PostgreSQL

**Рішення:**
1. Перевірте, чи запущений PostgreSQL:
   ```bash
   pg_isready
   ```

2. Перевірте файл `backend/.env`:
   ```env
   DATABASE_URL=postgresql://користувач:пароль@localhost:5432/kiosk_db
   ```

3. Перевірте, чи існує база даних:
   ```bash
   psql -l | grep kiosk_db
   ```

4. Якщо база не існує, створіть її:
   ```bash
   createdb kiosk_db
   ```

### Крок 3: Перевірте, чи створений користувач admin

Підключіться до бази даних:

```bash
psql kiosk_db
```

Перевірте наявність користувача:

```sql
SELECT username, full_name, role FROM users WHERE username = 'admin';
```

**Якщо користувача немає:**

1. Зупиніть backend (Ctrl+C)
2. Видаліть таблицю users (якщо потрібно):
   ```sql
   DROP TABLE IF EXISTS users CASCADE;
   ```
3. Запустіть backend знову - він автоматично створить користувача

**Або створіть користувача вручну:**

```sql
-- Встановіть Node.js модуль bcryptjs для хешування
-- Або використайте онлайн bcrypt генератор: https://bcrypt-generator.com/
-- Хеш для пароля "admin123" (10 rounds): $2a$10$rOzJqZqZqZqZqZqZqZqZqO

INSERT INTO users (username, password, full_name, role) 
VALUES ('admin', '$2a$10$rOzJqZqZqZqZqZqZqZqZqO', 'Адміністратор', 'admin');
```

**Краще рішення:** Просто перезапустіть backend - він автоматично створить користувача.

### Крок 4: Перевірте консоль браузера

1. Відкрийте DevTools (F12)
2. Перейдіть на вкладку "Console"
3. Спробуйте увійти
4. Перевірте помилки

**Типові помилки:**

- `Network Error` або `ECONNREFUSED` - backend не запущений
- `401 Unauthorized` - невірний логін/пароль або користувач не існує
- `500 Internal Server Error` - помилка на сервері (перевірте логи backend)

### Крок 5: Перевірте логи backend

В терміналі, де запущений backend, мають бути логи:

```
✅ Database initialized successfully
✅ Default admin user created (username: admin, password: admin123)
🚀 Server running on http://localhost:3001
```

**Якщо бачите помилку:**

```
❌ Database initialization error: ...
```

Це означає проблему з підключенням до БД. Перевірте:
- Чи запущений PostgreSQL
- Чи правильні дані в `backend/.env`
- Чи існує база даних `kiosk_db`

## Помилка: "Не вдалося підключитися до сервера"

### Рішення:

1. **Перевірте, чи запущений backend:**
   ```bash
   curl http://localhost:3001/api/health
   ```

2. **Перевірте порт:**
   - Backend має бути на порту 3001
   - Frontend має бути на порту 5173
   - Перевірте, чи не зайняті порти:
     ```bash
     lsof -ti:3001
     lsof -ti:5173
     ```

3. **Перевірте CORS:**
   - Backend має мати `app.use(cors())` в `backend/src/index.ts`
   - Це вже налаштовано в коді

4. **Перевірте proxy в Vite:**
   - Файл `frontend/vite.config.ts` має містити proxy настройки
   - Це вже налаштовано в коді

## Помилка: "Помилка сервера"

### Рішення:

1. **Перевірте логи backend** - там буде детальна інформація про помилку

2. **Перевірте базу даних:**
   ```bash
   psql kiosk_db -c "SELECT COUNT(*) FROM users;"
   ```

3. **Перезапустіть backend:**
   ```bash
   # Зупиніть (Ctrl+C)
   cd backend
   npm run dev
   ```

## Швидка діагностика

Виконайте ці команди по черзі:

```bash
# 1. Чи запущений PostgreSQL?
pg_isready

# 2. Чи існує база даних?
psql -l | grep kiosk_db

# 3. Чи працює backend?
curl http://localhost:3001/api/health

# 4. Чи є користувач admin?
psql kiosk_db -c "SELECT username FROM users WHERE username = 'admin';"

# 5. Перевірте файл .env
cat backend/.env
```

## Створення користувача вручну (якщо потрібно)

Якщо автоматичне створення не спрацювало, створіть користувача вручну:

```bash
cd backend
node -e "
const bcrypt = require('bcryptjs');
bcrypt.hash('admin123', 10).then(hash => {
  console.log('Хеш пароля:', hash);
  console.log('SQL команда:');
  console.log(\`INSERT INTO users (username, password, full_name, role) VALUES ('admin', '\${hash}', 'Адміністратор', 'admin');\`);
});
"
```

Потім виконайте SQL команду в PostgreSQL.

## Альтернатива: Використати SQLite для тестування

Якщо PostgreSQL не працює, можна тимчасово використати SQLite:

1. Встановіть пакет:
   ```bash
   cd backend
   npm install better-sqlite3
   ```

2. Змініть `backend/src/db/init.ts` для використання SQLite

**Примітка:** Для продакшн використання рекомендовано PostgreSQL.

## Отримати допомогу

Якщо проблема залишається:

1. Перевірте всі логи (backend та browser console)
2. Переконайтеся, що виконано всі кроки з QUICK_START.md
3. Перевірте версії Node.js та PostgreSQL

