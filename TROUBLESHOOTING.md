# 🔧 Діагностика проблем

## Білий екран після деплою

### Крок 1: Перевірте консоль браузера
1. Відкрийте сайт на Vercel
2. Натисніть **F12** (або Cmd+Option+I на Mac)
3. Перейдіть на вкладку **Console**
4. Подивіться на помилки (червоні повідомлення)

**Найчастіші помилки:**
- `Failed to fetch` → Backend не доступний або неправильний URL
- `CORS error` → Проблема з CORS налаштуваннями
- `VITE_API_URL не встановлено` → Не встановлено Environment Variable

### Крок 2: Перевірте Environment Variables в Vercel
1. Зайдіть в ваш проект на Vercel
2. Settings → Environment Variables
3. Переконайтеся, що є змінна `VITE_API_URL`
4. Значення має бути: `https://ваш-backend.onrender.com`
   - ✅ Правильно: `https://kiosk-backend.onrender.com`
   - ❌ Неправильно: `https://kiosk-backend.onrender.com/api`
5. Якщо змінної немає - додайте її
6. Після додавання - зробіть **Redeploy**

### Крок 3: Перевірте Network запити
1. Відкрийте **Network** tab (F12 → Network)
2. Перезавантажте сторінку (F5)
3. Подивіться на запити до `/api/...`
4. Якщо запити червоні (failed):
   - Перевірте, чи backend працює (відкрийте backend URL)
   - Перевірте CORS налаштування

### Крок 4: Перевірте, чи backend працює
1. Відкрийте backend URL в браузері (наприклад: `https://kiosk-backend.onrender.com`)
2. Має з'явитися JSON з інформацією про API
3. Якщо помилка - перевірте логи на Render.com

### Крок 5: Очистіть кеш
1. Натисніть **Ctrl+Shift+R** (Windows/Linux) або **Cmd+Shift+R** (Mac)
2. Або відкрийте сайт в режимі **інкогніто**

## Швидка перевірка

```bash
# 1. Перевірте backend
curl https://ваш-backend.onrender.com/api/health
# Має повернути: {"status":"ok"}

# 2. Перевірте frontend
# Відкрийте в браузері та перевірте консоль
```

## Якщо все ще не працює

1. **Перевірте логи Vercel:**
   - Зайдіть в Deployment → Logs
   - Подивіться на помилки під час збірки

2. **Перевірте логи Render:**
   - Зайдіть в ваш Web Service → Logs
   - Подивіться на помилки backend

3. **Перевірте базу даних:**
   - На Render.com → Database → Info
   - Переконайтеся, що база даних активна

4. **Спробуйте локально:**
   - Запустіть проект локально
   - Якщо працює локально - проблема в деплої
   - Якщо не працює - проблема в коді

## Типові помилки та рішення

### "Failed to fetch" або "Network Error"
- Backend не запущений або не доступний
- Неправильний `VITE_API_URL`
- Backend "заснув" (Render Free плану) - дочекайтеся 30-60 секунд

### "CORS policy" помилка
- Backend CORS налаштований правильно (`origin: '*'`)
- Перевірте, чи backend запущений
- Перевірте логи backend

### "VITE_API_URL не встановлено"
- Додайте Environment Variable в Vercel
- Зробіть Redeploy після додавання

### Білий екран без помилок
- Перевірте, чи є помилки в консолі
- Перевірте Network tab
- Спробуйте очистити кеш браузера

