# ⬇️ Інструкція: Даунгрейд з React 19 до React 18

## ✅ Що вже зроблено

1. ✅ Оновлено `package.json`:
   - `react`: `19.2.0` → `^18.3.1`
   - `react-dom`: `19.2.0` → `^18.3.1`
   - `@types/react`: `^19.2.5` → `^18.3.12`
   - `@types/react-dom`: `^19.2.3` → `^18.3.1`
   - Видалено `overrides` для React 19

2. ✅ Видалено future flags з `BrowserRouter` (для повної сумісності)

3. ✅ `main.tsx` вже використовує `createRoot` - це правильно для React 18

## 📋 Що потрібно зробити зараз

### Крок 1: Видалити node_modules та lock файли

```bash
cd frontend
rm -rf node_modules package-lock.json
```

**Або на Windows:**
```cmd
cd frontend
rmdir /s /q node_modules
del package-lock.json
```

### Крок 2: Встановити залежності заново

```bash
npm install
```

### Крок 3: Перевірити, що все працює

```bash
npm run dev
```

Відкрийте http://localhost:5173 і перевірте:
- [ ] Сайт завантажується без білого екрана
- [ ] Немає помилок в консолі браузера
- [ ] Графіки (recharts) працюють
- [ ] Навігація (react-router-dom) працює

### Крок 4: Зробити build для перевірки

```bash
npm run build
```

Якщо build успішний - все готово! 🎉

## 🔍 Перевірка помилок

Якщо після `npm install` з'являються помилки peer dependencies:

1. **Перевірте версії:**
   ```bash
   npm list react react-dom
   ```
   Мають бути версії `18.3.1` або близькі до цього.

2. **Якщо є конфлікти:**
   ```bash
   npm install --legacy-peer-deps
   ```

3. **Очистіть кеш npm:**
   ```bash
   npm cache clean --force
   rm -rf node_modules package-lock.json
   npm install
   ```

## 🚀 Після успішного тестування

1. Зробіть commit:
   ```bash
   git add frontend/package.json frontend/src/App.tsx
   git commit -m "Downgrade React from 19 to 18 for stability"
   git push
   ```

2. Vercel автоматично перезбере проект з новими залежностями

## 📝 Чому це потрібно було

- **React 19** ще в бета-версії і має проблеми сумісності
- **recharts** та інші бібліотеки не повністю підтримують React 19
- **React 18.3.1** - стабільна версія з повною підтримкою всіх бібліотек
- Це вирішить проблеми з білим екраном на деплої

## ✅ Очікуваний результат

Після даунгрейду:
- ✅ Немає peer dependencies warnings
- ✅ Немає білого екрана на деплої
- ✅ Всі бібліотеки працюють коректно
- ✅ Стабільна робота на production

